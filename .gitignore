# ── Credentials & secrets ──────────────────────────────────────────────────────
# NEVER commit these — they contain HIBC/Teleplan or AHCIP login credentials
teleplan/.env
teleplan/.env.*
hlink/.env
hlink/.env.*

# ── Teleplan runtime state ─────────────────────────────────────────────────────
teleplan/.seqnum

# ── Node modules ───────────────────────────────────────────────────────────────
teleplan/node_modules/
hlink/node_modules/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Conformance proof bundles (private test submission data) ───────────────────
# These contain real HIBC/AHCIP test PHNs and submission records.
# Keep locally; share with HIBC/AHCIP directly, not via public repo.
teleplan/conformance-proof/
hlink/conformance-proof/

# ── OS & editor artefacts ──────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
Thumbs.db
desktop.ini
*.lnk
.vscode/
.idea/
*.swp
*.swo

# ── Logs & temporaries ─────────────────────────────────────────────────────────
*.log
*.tmp
*.bak
